source 'https://rubygems.org'

gemspec

group :development do
  gem 'vagrant', :git => 'https://github.com/mitchellh/vagrant.git', :tag => 'v1.7.2'
end

group :plugins do
  gem 'awesome_print'
  gem 'vagrant-openstack-plugin', path: '.'
end
